apiVersion: n9/v1alpha
kind: Service
metadata:
  displayName: Submit Reservation
  labels:
    demo:
    - 'yes'
  name: submit-reservation
  project: consumer-reservations
spec:
  description: Service containing SLOs related to submitting reservations.
