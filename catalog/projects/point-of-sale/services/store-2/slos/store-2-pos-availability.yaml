apiVersion: n9/v1alpha
kind: SLO
metadata:
  displayName: Availability
  labels:
    location:
    - Detroit
  name: store-2-pos-availability
  project: point-of-sale
spec:
  budgetingMethod: Occurrences
  description: 'Note: This SLO tracks availability using the old legacy method'
  indicator:
    metricSource:
      kind: Direct
      name: dynatrace
      project: integrations
  objectives:
  - displayName: Point of Sale Availability
    name: v
    op: gt
    primary: false
    rawMetric:
      query:
        dynatrace:
          metricSelector: builtin:synthetic.http.availability.location.total:filter(and(or(in("dt.entity.http_check",entitySelector("type(http_check),entityName.equals(~"www.wienershirtzel.com~")"))))):splitBy():sort(value(auto,descending)):limit(20)
    target: 0.999
    value: 99
  service: store-2
  timeWindows:
  - count: 28
    isRolling: true
    unit: Day
