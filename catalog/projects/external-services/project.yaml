apiVersion: n9/v1alpha
kind: Project
metadata:
  displayName: External Services
  labels:
    composite-demo:
    - composite-demo
  name: external-services
spec:
  description: Project for software not deployed directly in our infrastructure.
